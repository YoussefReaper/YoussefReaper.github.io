<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Me - Creator of Auro</title>
    <meta name="description" content="Meet the creator of Auro, learn about the vision and journey behind this productivity platform.">
    <link rel="stylesheet" href="styles/main.css">
    <link rel="stylesheet" href="styles/modern-navigation.css">
    <link rel="stylesheet" href="styles/navigation-fix.css">
    <link rel="stylesheet" href="styles/emergency-sidebar-fix.css">
    <link rel="stylesheet" href="styles/dashboard.css">
    <link rel="stylesheet" href="styles/about.css">
    <link rel="stylesheet" href="styles/floating-nav.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body class="floating-nav-active" data-page="about-me">

    <!-- Modern Topbar -->
    <div class="topbar">
        <div class="topbar-left">
            <div class="page-breadcrumb">
                <a href="index.html" class="breadcrumb-item">Home</a>
                <span class="breadcrumb-separator">/</span>
                <span class="breadcrumb-current">About Me</span>
            </div>
        </div>
        
        <div class="topbar-nav">
            <a href="about-me.html" class="topbar-nav-item active">
                <i class="fas fa-user"></i>
                <span>About Me</span>
            </a>
            <a href="future-plans.html" class="topbar-nav-item">
                <i class="fas fa-rocket"></i>
                <span>Future Plans</span>
            </a>
            <a href="support-project.html" class="topbar-nav-item">
                <i class="fas fa-heart"></i>
                <span>Support Project</span>
            </a>
            <a href="feedbacks.html" class="topbar-nav-item">
                <i class="fas fa-comments"></i>
                <span>Feedbacks</span>
            </a>
        </div>
        
        <div class="topbar-actions">
            <div class="user-menu">
                <div class="user-trigger">
                    <div class="user-avatar" id="topbar-profile-pic">A</div>
                    <div class="user-info">
                        <span class="user-name" id="topbar-username">Auro User</span>
                        <span class="user-status">Active</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Hero Section -->
        <section class="hero-section about-hero">
            <div class="hero-content">
                <div class="creator-intro">
                    <div class="creator-avatar">
                        <div class="creator-image-placeholder">
                            <i class="fas fa-user-circle"></i>
                        </div>
                        <div class="creator-badge">
                            <i class="fas fa-code"></i>
                        </div>
                    </div>
                    <div class="creator-info">
                        <h1 class="creator-name">
                            <span class="greeting">Hi, I'm</span>
                            <span class="name-highlight">3maar</span>
                            <span class="wave">👋</span>
                        </h1>
                        <p class="creator-title">Creator & Developer of Auro</p>
                        <p class="creator-bio">
                            Passionate about building tools that help people achieve their dreams and live more productive lives. 
                            Welcome to my personal productivity universe!
                        </p>
                        <div class="creator-stats">
                            <div class="stat-item">
                                <span class="stat-number">1</span>
                                <span class="stat-label">Years Building Auro</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-number">10+</span>
                                <span class="stat-label">Features Created</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-number" id="user-count">∞</span>
                                <span class="stat-label">Dreams Supported</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Main About Grid -->
        <div class="dashboard-grid about-grid">
            <!-- User Profile Editor -->
            <div class="dashboard-card profile-editor-card">
                <div class="card-header">
                    <h3><i class="fas fa-user-edit"></i> Your Profile</h3>
                    <p>Customize your personal information and avatar</p>
                </div>
                <div class="card-content">
                    <div class="profile-editor">
                        <!-- Current Profile Display -->
                        <div class="current-profile-display">
                            <div class="profile-avatar-container">
                                <div class="profile-avatar-preview" id="profile-avatar-preview">
                                    <i class="fas fa-user"></i>
                                </div>
                                <button class="avatar-edit-btn" id="avatar-edit-btn">
                                    <i class="fas fa-camera"></i>
                                </button>
                                <input type="file" id="avatar-upload" accept="image/*" style="display: none;">
                            </div>
                            <div class="profile-info-display">
                                <h4 id="current-profile-name">Auro User</h4>
                                <p class="profile-role">Productivity Enthusiast</p>
                            </div>
                        </div>

                        <!-- Profile Edit Form -->
                        <div class="profile-edit-form">
                            <div class="form-group">
                                <label for="profile-name-input">Display Name</label>
                                <input type="text" id="profile-name-input" class="form-input" placeholder="Enter your display name" maxlength="50">
                            </div>
                            
                            <div class="form-group">
                                <label for="profile-bio-input">Bio (Optional)</label>
                                <textarea id="profile-bio-input" class="form-textarea" placeholder="Tell us about yourself..." maxlength="200" rows="3"></textarea>
                            </div>
                            
                            <div class="form-actions">
                                <button class="btn btn-primary" id="save-profile-btn">
                                    <i class="fas fa-save"></i> Save Changes
                                </button>
                                <button class="btn btn-secondary" id="reset-profile-btn">
                                    <i class="fas fa-undo"></i> Reset
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- My Story -->
            <div class="dashboard-card story-card">
                <div class="card-header">
                    <h3><i class="fas fa-book-open"></i> My Story</h3>
                </div>
                <div class="card-content">
                    <div class="story-content">
                        <p>
                            <strong>The Beginning:</strong> Like many of you, I struggled with staying organized and achieving my goals. 
                            I tried countless productivity apps, but nothing felt quite right. They were either too complex, 
                            too simple, or just didn't understand what I actually needed.
                        </p>
                        <p>
                            <strong>The Vision:</strong> I wanted something that could be my daily companion - something that 
                            understood my struggles, celebrated my wins, and helped me stay motivated even on the hardest days. 
                            That's when Auro was born.
                        </p>
                        <p>
                            <strong>The Journey:</strong> What started as a simple task manager has evolved into a comprehensive 
                            productivity ecosystem. Every feature in Auro comes from real needs - either mine or from the amazing 
                            community that has grown around this project.
                        </p>
                        <div class="story-quote">
                            <i class="fas fa-quote-left"></i>
                            <p>"I believe that everyone has the potential to achieve greatness. Sometimes we just need the right tools and a little encouragement."</p>
                            <span class="quote-author">- 3maar</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Connect With Me -->
            <div class="dashboard-card connect-card">
                <div class="card-header">
                    <h3><i class="fas fa-handshake"></i> Connect With Me</h3>
                </div>
                <div class="card-content">
                    <div class="social-links">
                        <a href="#" class="social-link discord" target="_blank">
                            <div class="social-icon">
                                <i class="fab fa-discord"></i>
                            </div>
                            <div class="social-info">
                                <h4>Discord Community</h4>
                                <p>Join our supportive community</p>
                            </div>
                        </a>
                        <a href="#" class="social-link github" target="_blank">
                            <div class="social-icon">
                                <i class="fab fa-github"></i>
                            </div>
                            <div class="social-info">
                                <h4>GitHub</h4>
                                <p>Check out the code & contribute</p>
                            </div>
                        </a>
                        <a href="#" class="social-link twitter" target="_blank">
                            <div class="social-icon">
                                <i class="fab fa-twitter"></i>
                            </div>
                            <div class="social-info">
                                <h4>Twitter</h4>
                                <p>Follow for updates & tips</p>
                            </div>
                        </a>
                        <a href="#" class="social-link email" href="mailto:3maar@example.com">
                            <div class="social-icon">
                                <i class="fas fa-envelope"></i>
                            </div>
                            <div class="social-info">
                                <h4>Email</h4>
                                <p>Direct contact for partnerships</p>
                            </div>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="scripts/customization-loader.js"></script>
    <script src="scripts/profile-bootstrapper.js"></script>
    <script src="scripts/global-navigation.js"></script>
    <script src="scripts/storage-manager.js"></script>
    <script src="scripts/global-profile-manager.js"></script>
    <script src="scripts/global-stats-manager.js"></script>
    <script>
        // Function to load user profile data from localStorage
        function loadUserProfile() {
            // Get user information from localStorage
            let userName = localStorage.getItem('userName') || 'Auro User';
            let userImage = localStorage.getItem('userProfileImage');
            let userBio = localStorage.getItem('userBio') || '';
            
            console.log('Loading user profile in page script:', { userName, userImage, userBio });
            
            // Update username in topbar
            const usernameElement = document.getElementById('topbar-username');
            if (usernameElement) {
                usernameElement.textContent = userName;
            }
            
            // Update avatar in topbar
            const profilePicElement = document.getElementById('topbar-profile-pic');
            if (profilePicElement) {
                if (userImage) {
                    // If we have an image, show it
                    profilePicElement.innerHTML = `<img src="${userImage}" alt="${userName}">`;
                    profilePicElement.classList.add('has-image');
                } else {
                    // Otherwise show initial
                    profilePicElement.textContent = userName.charAt(0).toUpperCase();
                    profilePicElement.classList.remove('has-image');
                }
            }
            
            // Update profile editor elements
            const profileNameInput = document.getElementById('profile-name-input');
            const profileBioInput = document.getElementById('profile-bio-input');
            const currentProfileName = document.getElementById('current-profile-name');
            const profileAvatarPreview = document.getElementById('profile-avatar-preview');
            
            if (profileNameInput) profileNameInput.value = userName;
            if (profileBioInput) profileBioInput.value = userBio;
            if (currentProfileName) currentProfileName.textContent = userName;
            
            if (profileAvatarPreview) {
                if (userImage) {
                    profileAvatarPreview.innerHTML = `<img src="${userImage}" alt="${userName}">`;
                    profileAvatarPreview.classList.add('has-image');
                } else {
                    profileAvatarPreview.innerHTML = `<i class="fas fa-user"></i>`;
                    profileAvatarPreview.classList.remove('has-image');
                }
            }
        }

        // Profile Editor Functionality
        function initializeProfileEditor() {
            const avatarEditBtn = document.getElementById('avatar-edit-btn');
            const avatarUpload = document.getElementById('avatar-upload');
            const saveProfileBtn = document.getElementById('save-profile-btn');
            const resetProfileBtn = document.getElementById('reset-profile-btn');
            const profileNameInput = document.getElementById('profile-name-input');
            const profileBioInput = document.getElementById('profile-bio-input');

            // Avatar upload functionality
            if (avatarEditBtn && avatarUpload) {
                avatarEditBtn.addEventListener('click', () => {
                    avatarUpload.click();
                });

                avatarUpload.addEventListener('change', (e) => {
                    const file = e.target.files[0];
                    if (file) {
                        // Validate file type
                        if (!file.type.startsWith('image/')) {
                            showProfileMessage('Please select a valid image file.', 'error');
                            return;
                        }

                        // Validate file size (max 5MB)
                        if (file.size > 5 * 1024 * 1024) {
                            showProfileMessage('Image size must be less than 5MB.', 'error');
                            return;
                        }

                        const reader = new FileReader();
                        reader.onload = (e) => {
                            const imageDataUrl = e.target.result;
                            
                            // Update preview
                            const profileAvatarPreview = document.getElementById('profile-avatar-preview');
                            if (profileAvatarPreview) {
                                profileAvatarPreview.innerHTML = `<img src="${imageDataUrl}" alt="Profile Preview">`;
                                profileAvatarPreview.classList.add('has-image');
                            }
                            
                            showProfileMessage('Image uploaded! Remember to save your changes.', 'success');
                        };
                        reader.readAsDataURL(file);
                    }
                });
            }

            // Save profile functionality
            if (saveProfileBtn) {
                saveProfileBtn.addEventListener('click', () => {
                    const name = profileNameInput?.value.trim();
                    const bio = profileBioInput?.value.trim();
                    
                    // Validate name
                    if (!name || name.length < 2) {
                        showProfileMessage('Display name must be at least 2 characters long.', 'error');
                        return;
                    }

                    // Get current avatar
                    const profileAvatarPreview = document.getElementById('profile-avatar-preview');
                    const avatarImg = profileAvatarPreview?.querySelector('img');
                    const avatarDataUrl = avatarImg?.src || null;

                    try {
                        // Save to localStorage
                        localStorage.setItem('userName', name);
                        localStorage.setItem('userBio', bio);
                        
                        if (avatarDataUrl && avatarDataUrl.startsWith('data:')) {
                            localStorage.setItem('userProfileImage', avatarDataUrl);
                        }

                        // Update display
                        const currentProfileName = document.getElementById('current-profile-name');
                        if (currentProfileName) currentProfileName.textContent = name;

                        // Update topbar
                        loadUserProfile();

                        showProfileMessage('Profile saved successfully!', 'success');
                        
                        // Trigger global profile update
                        if (window.globalProfileManager && window.globalProfileManager.updateAllProfiles) {
                            window.globalProfileManager.updateAllProfiles();
                        }
                        
                    } catch (error) {
                        console.error('Error saving profile:', error);
                        showProfileMessage('Error saving profile. Please try again.', 'error');
                    }
                });
            }

            // Reset profile functionality
            if (resetProfileBtn) {
                resetProfileBtn.addEventListener('click', () => {
                    if (confirm('Are you sure you want to reset your profile? This will restore default values.')) {
                        try {
                            // Clear localStorage
                            localStorage.removeItem('userName');
                            localStorage.removeItem('userBio');
                            localStorage.removeItem('userProfileImage');
                            
                            // Reset form
                            if (profileNameInput) profileNameInput.value = 'Auro User';
                            if (profileBioInput) profileBioInput.value = '';
                            
                            // Reset preview
                            const profileAvatarPreview = document.getElementById('profile-avatar-preview');
                            if (profileAvatarPreview) {
                                profileAvatarPreview.innerHTML = `<i class="fas fa-user"></i>`;
                                profileAvatarPreview.classList.remove('has-image');
                            }
                            
                            // Update display
                            loadUserProfile();
                            
                            showProfileMessage('Profile reset successfully!', 'success');
                            
                        } catch (error) {
                            console.error('Error resetting profile:', error);
                            showProfileMessage('Error resetting profile. Please try again.', 'error');
                        }
                    }
                });
            }
        }

        // Show profile messages
        function showProfileMessage(message, type) {
            // Remove existing messages
            const existingMessages = document.querySelectorAll('.profile-message');
            existingMessages.forEach(msg => msg.remove());

            // Create new message
            const messageDiv = document.createElement('div');
            messageDiv.className = `profile-message ${type}`;
            messageDiv.innerHTML = `
                <i class="fas fa-${type === 'success' ? 'check-circle' : 'exclamation-circle'}"></i>
                ${message}
            `;

            // Insert before profile editor
            const profileEditor = document.querySelector('.profile-editor');
            if (profileEditor) {
                profileEditor.parentNode.insertBefore(messageDiv, profileEditor);
            }

            // Auto-remove after 5 seconds
            setTimeout(() => {
                messageDiv.remove();
            }, 5000);
        }

        // Load user profile when page loads
        document.addEventListener('DOMContentLoaded', function() {
            loadUserProfile();
            initializeProfileEditor();
        });
        
        // Initialize sidebar toggle - improved version for expandable sidebar
        document.addEventListener('DOMContentLoaded', function() {
            const sidebar = document.getElementById('main-sidebar');
            const toggleBtn = document.getElementById('sidebar-toggle');
            
            if (toggleBtn && sidebar) {
                toggleBtn.addEventListener('click', function(e) {
                    e.stopPropagation(); // Prevent event bubbling
                    sidebar.classList.toggle('expanded');
                    
                    // For mobile
                    if (window.innerWidth <= 768) {
                        sidebar.classList.toggle('open');
                    }
                });
            }
            
            // Close sidebar when clicking outside on mobile
            document.addEventListener('click', function(e) {
                if (window.innerWidth <= 768 && 
                    sidebar && 
                    !sidebar.contains(e.target) && 
                    sidebar.classList.contains('open')) {
                    sidebar.classList.remove('open');
                }
            });
        });
    </script>

    <!-- Floating Navigation Background -->
    <div class="floating-nav-background" id="floating-nav-background"></div>

    <!-- Floating Navigation Hub -->
    <div class="floating-nav-hub" id="floating-nav-hub">
        <button class="nav-hub-button" id="nav-hub-toggle">
            <i class="fas fa-th"></i>
        </button>
        
        <div class="nav-options">
            <a href="index.html" class="nav-option">
                <i class="fas fa-home"></i>
                <span>Home</span>
            </a>
            <a href="plans.html" class="nav-option">
                <i class="fas fa-map"></i>
                <span>Plans</span>
            </a>
            <a href="chat.html" class="nav-option registered-only">
                <i class="fas fa-message"></i>
                <span>Chat</span>
            </a>
            <a href="tasks-new.html" class="nav-option registered-only">
                <i class="fas fa-tasks"></i>
                <span>Tasks</span>
            </a>
            <a href="schedule.html" class="nav-option registered-only">
                <i class="fas fa-calendar-alt"></i>
                <span>Schedule</span>
            </a>
            <a href="achievements.html" class="nav-option registered-only">
                <i class="fas fa-trophy"></i>
                <span>Achievements</span>
            </a>
            <a href="super-tracker.html" class="nav-option registered-only">
                <i class="fas fa-chart-line"></i>
                <span>Tracker</span>
            </a>
            <a href="customization.html" class="nav-option registered-only">
                <i class="fas fa-heart"></i>
                <span>Customize</span>
            </a>
            <a href="about-me.html" class="nav-option current-page">
                <i class="fas fa-user-circle"></i>
                <span>About</span>
            </a>
            <a href="settings.html" class="nav-option">
                <i class="fas fa-cog"></i>
                <span>Settings</span>
            </a>
        </div>
    </div>

    <!-- Include required scripts -->
    <script src="scripts/floating-navigation.js"></script>
    <script src="scripts/global-stats-manager.js"></script>
    <script src="scripts/global-profile-manager.js"></script>
    <script src="scripts/global-navigation.js"></script>
    <script src="scripts/customization-loader.js"></script>
    <script src="scripts/system.js"></script>
</body>
</html>
